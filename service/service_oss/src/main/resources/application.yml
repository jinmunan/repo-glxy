server:
  port: 8120 # 服务端口

spring:
  profiles:
    active: dev # 环境设置
  application:
    name: service-oss # 服务名
  # spring:
  # nacos服务注册
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    keyid: LTAI5tCU4a3RoSNhkY652rtk
    keysecret: icC5uoPAWiMsB013pqLaQBtUJjaS1t
    #bucket可以在控制台创建，也可以使用java代码创建，注意先测试bucket是否已被占用
    bucketname: glxy-picture-server


# 统一日志处理
logging:
  level:
    root: INFO
